# Local environment overrides (no production secrets in git)
# Copy to .env if needed for future tooling.

# AppMetrica — leave empty until release configuration is ready
EXPO_PUBLIC_APPMETRICA_API_KEY=

# Yandex Mobile Ads (РСЯ) — leave empty until release configuration is ready
EXPO_PUBLIC_ADS_BANNER_UNIT_ID=
EXPO_PUBLIC_ADS_INTERSTITIAL_UNIT_ID=
